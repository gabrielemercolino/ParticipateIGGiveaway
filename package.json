{
  "name": "ig-auto-giveaway",
  "version": "8.0.0",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "minify": "node build.js --minify",
    "watch": "node build.js --watch"
  },
  "devDependencies": {
    "@types/tampermonkey": "^5.0",
    "esbuild": "^0.25.4",
    "typescript": "^5.0.0"
  }
}
